POSTGRES_DB=personalfinance
POSTGRES_USER=personalfinance
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5433

# Spring Boot loads this local file from either the repository root or backend directory.
DB_HOST=localhost
DB_PORT=5433
DB_NAME=personalfinance
DB_USER=personalfinance
DB_PASSWORD=change-me

# Local SMTP catcher (e.g. Mailpit). For Gmail, use port 587 and set auth/TLS to true.
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_AUTH=false
SMTP_STARTTLS_ENABLE=false
SMTP_STARTTLS_REQUIRED=false
MAIL_FROM=no-reply@localhost
